# GlamGo Agent Bridge

This repo uses an external Anti-Gravity knowledge base at:
`C:\Users\Swift America\.gemini\antigravity\`

When working in this workspace, use `.agents/antigravity-bridge.md` as the local router for:
- which workflow to use for the task type
- which skill set to load for the domain
- which context files are required for grounded decisions

Do not duplicate the full global system in this repo.
Keep the source of truth in `C:\Users\Swift America\.gemini\antigravity\`.
